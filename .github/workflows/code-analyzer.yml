name: Salesforce Code Analyzer

on:
  pull_request:
    branches:
      - development # Run on PRs targeting the 'development' branch
    types: [opened, edited, synchronized, reopened]
  push:
    branches:
      - development # Run on a merge or push to the 'development' branch

jobs:
  salesforce-code-analyzer-scan:
    # ... rest of your job remains the same ...
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: 'Check out source code'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run Salesforce Code Analyzer'
        uses: forcedotcom/run-code-analyzer@v2
        id: scan-step
        with:
          run-arguments: '--view detail --output-file sfca_results.json'
      - name: 'Check for violations and fail job if necessary'
        if: ${{ steps.scan-step.outputs.num-violations > 0 }}
        run: |
          echo "Salesforce Code Analyzer found ${{ steps.scan-step.outputs.num-violations }} violations."
          exit 1
